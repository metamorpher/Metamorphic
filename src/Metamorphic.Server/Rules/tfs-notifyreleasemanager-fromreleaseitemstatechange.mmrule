---
    name: "tfs-notifyreleasemanager-fromreleaseitemstatechange"
    description: "Notifies the release manager when a release item changes state from NEW to INITIATE."
    enabled: true

    signal:
        id: "WorkItemChange"
        parameters:
            projectcollection: "tfs_team_project_collection"
            projectname: "tfs_project_name"
            workitemtype: "workitem_type"
            id: "workitem_id"
            user: "workitem_user"
            state: "workitem_state"
            title: "workitem_title"

    condition:
        - name: "workitemtype"
          type: "equals"
          pattern : "Release"
        - name: "state"
          type: "equals"
          pattern : "Initiation"

    action:
        id: "powershell"
        parameters:
            scriptfile: "notify-releasemanager.ps1"
            arguments: "-projectcollection \"{{signal.projectcollection}}\" -project \"{{signal.projectname}}\" -id {{signal.id}} -user \"{{signal.user}}\" -Verbose"