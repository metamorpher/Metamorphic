---
    name: "Name"
    description: "Description"
    enabled: true

    signal:
        id: "Signal"
        parameters:
            bar: "stuff"

    condition:
        - name: "bar"
          type: "notmatchregex"
          pattern : "(.*)"

    action:
        id: "Action"
        parameters:
            foo: "bar"