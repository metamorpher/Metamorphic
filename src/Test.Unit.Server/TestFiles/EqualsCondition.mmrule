---
    name: "Name"
    description: "Description"
    enabled: true

    signal:
        id: "Signal"
        parameters:
            bar: "stuff"

    condition:
        - name: "bar"
          type: "equals"
          pattern : "a"

    action:
        id: "Action"
        parameters:
            foo: "bar"