---
    name: "Name"
    description: "Description"
    enabled: true

    signal:
        id: "Signal"
        parameters:
            foo: 20

    condition:
        - name: "foo"
          type: "lessthan"
          pattern : 15

    action:
        id: "Action"
        parameters:
            bar: "{{signal.foo}}"