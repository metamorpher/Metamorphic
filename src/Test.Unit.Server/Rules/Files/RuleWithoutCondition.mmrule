---
    name: "Name"
    description: "Description."
    enabled: true

    signal:
        type: "Signal"
        parameters:
            foo: 1

    action:
        id: "Action"
        parameters:
            bar: "{{signal.foo}}"