---
    name: "Name"
    description: "Description."
    enabled: true

    signal:
        type: "Signal"
        parameters:
            foo: "bar"

    condition:
        - name: "foo"
          type: "equals"
          pattern : 

    action:
        id: "Action"
        parameters:
            foo: "bar"
            baz: "{{signal.foo}}"