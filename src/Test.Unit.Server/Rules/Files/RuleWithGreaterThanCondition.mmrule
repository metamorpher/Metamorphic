---
    name: "Name"
    description: "Description."
    enabled: true

    signal:
        type: "Signal"
        parameters:
            foo: 1

    condition:
        - name: "foo"
          type: "greaterthan"
          pattern : 5

    action:
        id: "Action"
        parameters:
            bar: "{{signal.foo}}"