---
    name: "Name"
    description: "Description."
    enabled: true

    signal:
        id: "Signal"
        parameters:
            foo: "bar"

    condition:
        - name: "foo"
          type: "equals"
          pattern : "bar"

    action:
        id: ""
        parameters:
            foo: "bar"
            baz: "{{signal.foo}}"