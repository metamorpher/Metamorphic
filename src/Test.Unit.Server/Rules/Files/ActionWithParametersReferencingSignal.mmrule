---
    name: "Name"
    description: "Description"
    enabled: true

    signal:
        type: "Signal"
        parameters:
            bar: "stuff"

    action:
        id: "Action"
        parameters:
            foo: "{{signal.bar}}"