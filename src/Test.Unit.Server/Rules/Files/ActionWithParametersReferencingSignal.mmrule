---
    name: "Name"
    description: "Description"
    enabled: true

    signal:
        id: "Signal"
        parameters:
            bar: "stuff"

    action:
        id: "Action"
        parameters:
            foo: "{{signal.bar}}"