---
    name: "rule_name"
    description: "Rule description."
    enabled: true

    signal:
        type: "Signal"
        parameters:
            bar: "stuff"

    condition:
        - name: "bar"
          type: "notmatchregex"
          pattern : "(.*)"

    action:
        id: "Action"
        parameters:
            foo: "bar"