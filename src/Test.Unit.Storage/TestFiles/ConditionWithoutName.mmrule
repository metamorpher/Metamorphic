---
    name: "Name"
    description: "Description"
    enabled: true

    signal:
        id: "Signal"
        parameters:
            foo: "bar"

    condition:
        - name: ""
          type: "equals"
          pattern : "bar"

    action:
        id: "Action"
        parameters:
            foo: "bar"
            baz: "{{signal.foo}}"