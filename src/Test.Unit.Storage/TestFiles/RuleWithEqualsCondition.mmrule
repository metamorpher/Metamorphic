---
    name: "Name"
    description: "Description"
    enabled: true

    signal:
        id: "Signal"
        parameters:
            foo: 1

    condition:
        - name: "foo"
          type: "equals"
          pattern : 10

    action:
        id: "Action"
        parameters:
            bar: "{{signal.foo}}"